{
  "name": "google-cactus",
  "version": "1.1.1",
  "description": "Extension for easing this module using for only decode google credentials in client side rendering. This module is to facilitate the acquisition of credentials when using \"@react-oauth/google\", so it is only compatible with the method of that component used in react.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./scripts/build.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
